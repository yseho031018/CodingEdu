# .env 로 복사한 뒤 값을 채우세요(.env 는 Git에 올리지 마세요).
# 아래는 TiDB Cloud(MySQL 프로토콜) 예시입니다. 실제 호스트·계정은 대시보드에서 확인하세요.
DB_URL=jdbc:mysql://<gateway-host>.tidbcloud.com:4000/<database>?sslMode=VERIFY_IDENTITY&serverTimezone=UTC
DB_USERNAME=<tidb_user>
DB_PASSWORD=<tidb_password>
